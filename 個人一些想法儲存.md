

## 💡 關於您在筆記中提出的 2.5D 地圖物件問題探討

針對您在 `個人一些想法儲存.md` 中提到的三個問題，我為您進行了架構評估與設計建議：

### 問題 1：當前的結構要擴充成支援這樣的機能是否複雜？
**回答：完全不複雜，現有架構非常適合擴充！**
- 目前專案的職責劃分很乾淨（Domain 地圖資料、Selection 素材選擇、Rendering 渲染分層分離）。
- `AGENTS.md` 中原本就有規範概念：「*PNG files are terrain stamps, not tile textures. Assets are normally rendered without clipping to the hex boundary.*」
- 因此引入「地圖物件（Prop / Object）」只是在現有的 Terrain 地表層之上，增加一個獨立的物件層與 Y-Sort 排序機制。

---

### 問題 2：你會如何設計讓它達到 2.5D 的遮擋效果？
在 2.5D 斜視角／等角六角格中，要達到前後覆蓋的立體感，核心關鍵是 **「深度排序（Y-Sorting / Depth Sorting）」** 與 **「錨點（Anchor Point / Pivot）」**：

1. **底錨點（Bottom-Center Pivot）**：
   - 地圖物件（如高聳的樹木、大石頭、建築物）的基準定位點不是圖片中心，而是其**接觸地面的底部中心**（對應該六角格中心）。
   - 繪製時：頂部會自然向上超出六角格邊框，覆蓋到上方（Row 較小）的格子。
2. **渲染循序（Y-Sort）**：
   - 依據座標由上而下（小 Row $\to$ 大 Row）繪製：
     - 上方（後排）的物件先畫。
     - 下方（前排）的物件後畫，其頂部會自然遮擋住上方格子的底部，產生非常逼真的 2.5D 前後層次深度！
3. **無裁切渲染（Unclipped Rendering）**：
   - 地形底色/地表貼皮使用 Hex 多邊形裁切（Clip）；
   - 地圖物件層（Props / Objects）**不裁切（No Clip）**，允許自然延伸出格線之外。

---

### 問題 3：地圖物件必須與地形的資產分開，所以應該是不同圖層？
**回答：是的，絕對必須是不同圖層！**

在架構設計上建議分為三層（Layered Architecture）：

```
┌────────────────────────────────────────────────────────┐
│  Layer 3: 地圖物件層 (Prop / Object Layer)              │
│  - 樹木、岩石、城鎮模型 (PNG 印章，不裁切，Y-Sort 深度覆蓋) │
├────────────────────────────────────────────────────────┤
│  Layer 2: 網格指示層 (Hex Grid & Selection Overlay)     │
│  - 六角格線、游標懸停高亮、座標標籤 (可自訂開關在物件上或下)│
├────────────────────────────────────────────────────────┤
│  Layer 1: 地形底表層 (Base Terrain Layer)               │
│  - 草地、水域、沙地底色與平鋪 Stamp (Hex 多邊形裁切)       │
└────────────────────────────────────────────────────────┘
```

- **資料結構分離**：
  - `TerrainMap`：記錄各格的基礎地形（例如 Grass, Water, Sand）。
  - `PropMap` / `ObjectMap`：記錄各格放置的地圖物件（例如 `Tree_Oak_01`, `Rock_Big`，可為空）。
- **資產庫分離**：
  - `TerrainAssetRegistry`：管理地表印章。
  - `PropAssetRegistry`：管理立體地圖物件（支援設定錨點 Offset、隨機散佈權重等）。

---

如果您希望下一步開始加入 **2.5D 地圖物件層（Props Layer）** 或支援物件匯入與預覽，請隨時告訴我，我很樂意為您規劃並逐步實現！